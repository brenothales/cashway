<% if current_user %>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
     <%= image_tag("logo.png", height:35, class: 'mr-5') %> Gerencial</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Clientes', users_path, class:'nav-link active' if current_user %>
        </li>
        <li class="nav-item">
          <a href="http://localhost:3001" target="_blank" class="nav-link">Banking</a>
        </li>
      </ul>

      <ul class="navbar-nav">
 
          <li class="nav-item">
            <a href="#" class="nav-link"> <%= current_user.admin ? "Gerente:" : "Cliente:" %> <%= current_user.name.titleize %></a>
          </li>
          <li class="nav-item">
            <%= link_to "Conta: #{current_user.account}", edit_user_path(current_user), class:"nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sair', destroy_user_session_path, method: :delete, class:"nav-link" %>
          </li>
          <li class="nav-item">

      </ul>
    </div>
  </div>
</nav>
<% end %>